# Вправа-20

A simple Python exercise (exercise 20) that prints a greeting.

## Project Structure

- `Вправа-20.py` — Main Python script (prints "Вправа 20")

## Running

The workflow "Start application" runs `python 'Вправа-20.py'` and outputs to the console.

## Language

- Python 3.12
